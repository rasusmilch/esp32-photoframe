# Decision log

Entries are append-only. A changed decision receives a new entry that explicitly supersedes the old one.

DEC-001 through DEC-017 are **Accepted**, dated 2026-07-28. Later entries carry their acceptance date. All entries require traceability through `docs/VALIDATION.md`.

| ID | Decision and context | Consequences | Requirements / validation obligation |
|---|---|---|---|
| DEC-001 | Local slideshow is offline-first and independently functional. | Cold boot/display/navigation/schedule cannot depend on Wi-Fi. | REQ-OFFLINE-*; offline matrices |
| DEC-002 | Wi-Fi is additive, not a local-mode prerequisite. | Network services cannot own local lifecycle. | REQ-WIFI-001, REQ-COMPAT-*; boot tests |
| DEC-003 | Retry is non-destructive, serialized, and background. | Preserve credentials; no overlap/reboot loop. | REQ-WIFI-003/004; fake-clock/failure tests |
| DEC-004 | Default retry is configurable and 15 minutes. | Configuration and scheduling require tests. | REQ-WIFI-002 |
| DEC-005 | `wifi.txt` is deleted only after durable commit/verification. | Recover deletion/power failures without repeat import. | REQ-CREDENTIAL-*; fault injection |
| DEC-006 | Preserve all six current boards. | No silent feature/board removal. | REQ-PLATFORM-001/003; build/hardware matrices |
| DEC-007 | E1002/E1004 are priority targets. | First complete physical matrices target them. | REQ-PLATFORM-002 |
| DEC-008 | Controls mean refresh, previous, next, and long-press clear as mapped in hardware authority. | Insufficient controls may be explicit; no board removal. | REQ-CONTROL-*, REQ-NAV-*; button tests |
| DEC-009 | Physical inputs are separate from semantic actions. | HAL role names cannot dictate global policy. | REQ-CONTROL-002; architecture review |
| DEC-010 | Direct SD JPEG discovery is deferred. | BMP/PNG/EPDGZ local; JPEG uses upload/conversion. | REQ-IMAGE-001 |
| DEC-011 | Preserve URL, HA, OTA, web/API, mDNS, and conversion workflows. | Offline changes require compatibility checks. | REQ-COMPAT-* |
| DEC-012 | Secrets are redacted while safe reason diagnostics remain. | Never log credentials/forms; log disconnect reasons. | REQ-SEC-*, REQ-WIFI-005; log tests |
| DEC-013 | Codex receipts use exactly one `~~~text` fence with no nested fence. | Receipt formatting is repository authority. | `AGENTS.md`; receipt review |
| DEC-014 | The available local checkout is authoritative when network/GitHub is unavailable. | Work proceeds using inspected local evidence. | `AGENTS.md`; inspection receipt |
| DEC-015 | Local/remote SHA differences are expected and do not independently block work. | Record the actual local SHA used. | `AGENTS.md`; validation metadata |
| DEC-016 | Standalone epoch-fence physical validation uses one accepted representative execution per scenario, with diagnostic reruns only after failure, anomaly, timing sensitivity, relevant implementation change, or intentionally different environment; supersedes arbitrary repeated-identical-run requirements. | B/C/D/E/G accepted E1002 evidence opens the production connectivity lifecycle integration prerequisite while A/F remain environment-limited debt; failed C1/E1 runs remain evidence. This is not full E1002/E1004 hardware validation or release closure. | `docs/VALIDATION.md`; host synthetic A–G coverage; external physical evidence provenance |
| DEC-017 | Production Wi-Fi lifecycle operations have one serialized owner adapting the pure connectivity policy and validated stop/fence/quarantine model. | Normal boot, provisioning, scan, replacement, performance mode, and sleep teardown submit through `connectivity_runtime`; compatibility APIs publish state but cannot become alternative driver owners. | REQ-WIFI-001/002/003/004/005, REQ-CREDENTIAL-001, REQ-PROVISION-003, DEC-002/003/016; host lifecycle tests; production hardware validation pending |
| DEC-018 (Accepted 2026-09-12) | Durable flash persistence is reserved for operator/configuration/security state and explicitly power-loss-durable product state. Frequently changing runtime bookkeeping stays in RAM, may optionally use RTC-retained memory across deep sleep when useful, or is reconstructed after cold boot; deep-sleep continuity alone does not justify recurring NVS writes. | Rework or remove NVS-backed slideshow position (`last_idx`, `last_image`) and SNTP/OTA last-run (`periodic/sntp_sync`, `periodic/ota_check`) behavior in follow-up implementation. Cold boot may forget these ephemeral values, RTC retention is optional, and future recurring durable runtime writes require explicit authority and a correctness justification. Durable credentials and configuration remain persistent. | REQ-STATE-001/002/003/004/005, REQ-NAV-002; future host/source and, where RTC/deep sleep is used, hardware validation required by `docs/TESTING.md` and recorded in `docs/VALIDATION.md` |
| DEC-019 (Accepted 2026-09-12; supersedes DEC-006) | Supported-board preservation is defined by the authoritative `boards/boards.json` catalog and supported build matrix, not a hard-coded count. Adding an accepted/upstream-supported board to that catalog expands the preservation set; no cataloged board may be silently removed. | Normative requirements use no fixed board count. Build, test, and current documentation coverage follow the catalog; board-specific features and exceptions remain explicit. E1002/E1004 priority is unchanged. Catalog presence does not constitute firmware-build or physical-hardware validation, including for newly added boards. | REQ-PLATFORM-001/003; all-catalog build matrix, board-specific review, and applicable hardware validation obligations in `docs/TESTING.md` and `docs/VALIDATION.md` |
| DEC-020 (Accepted 2026-09-13) | The internal canonical identity of a local slideshow image is its exact, case-preserved `album/filename` path relative to `IMAGE_DIRECTORY`. Eligible regular BMP/PNG/EPDGZ files from enabled albums form one logical inventory, deduplicated by identity and ordered by case-sensitive bytewise `strcmp`, independent of `readdir()` and enabled-album order. The navigation cursor is the identity of the last successfully committed local position, not a numeric index, absolute path, basename, URL, `current_image`, `.current.lnk`, or NVS key. | With no cursor, NEXT selects first and PREVIOUS last. With an exact cursor, NEXT/PREVIOUS select the adjacent identity with wrap. With a missing cursor, NEXT selects the first identity strictly greater (or wraps first) and PREVIOUS the greatest strictly less (or wraps last); empty has no target and one item wraps to itself. Internal SoC RTC-retained memory will carry a bounded, explicitly versioned identity only across genuine deep-sleep wakes; absent, malformed, incompatible, or non-deep-sleep state is ignored and uses no-cursor behavior. A missing but structurally valid retained identity remains usable for insertion-point resolution. No durable/NVS cursor is permitted, and display-publication state remains separate. `last_idx` remains NVS-backed implementation debt until this target is implemented. | REQ-NAV-001/002, REQ-STATE-002/003/004/005, REQ-OFFLINE-001; dependency-free inventory/navigation tests, retained-state validity/reset qualification tests, no-NVS source/storage review, and physical deep-sleep validation |
